import { SearchHistoryPage } from "@/components/history/search-history-page";

export const metadata = {
  title: "Search History | PropertyWalay",
};

export default function HistoryPage() {
  return <SearchHistoryPage />;
}
